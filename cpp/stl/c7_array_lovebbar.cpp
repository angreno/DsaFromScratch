#include<bits/stdc++.h>
using namespace std;
int main(){
  // to declare sequence container array

  // syntax  type of container <data type , total members  if its  not dynamic >  variable name 

  array <int , 4> a = {1,2,3,4};
  cout<<" the size of array is " <<a.size()<<" array is empty or not ? "<<a.empty()<<" front element is "<<a.front()<<" last element is "<<a.back()<<" element at index 0 is "<<a.at(0);
  

}